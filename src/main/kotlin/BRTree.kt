class BRTree<K: Comparator<K>, T>(): BinaryTree<K, T, BRNode<K, T>> {
    private var root:BRNode<K, T>?=null

    private fun leftRotation(node: AVLNode<K, T>) {}
    private fun rightRotation(node: AVLNode<K, T>) {}
}